# Story 2.2 — `GET /customers/count`

## Status
Done

## Story
Mint fogyasztó, szeretném tudni az ügyfelek számát.

## Acceptance Criteria
1. `{ "count": <egész> }` a tábla tényleges sorszámával (FR6).
2. Hibánál `500` JSON hibaválasz (NFR3).

## Tasks / Subtasks
- [x] `GET /customers/count` a `src/server.ts`-ben — `prisma.customer.count()`
- [x] try/catch → `500 { error }`
- [x] Integrációs teszt: count === 15

## Dev Notes
Async route hibakezelés try/catch-csel (NFR3).

## Dev Agent Record
Implementálva; integrációs teszt igazolja a 15-öt.
