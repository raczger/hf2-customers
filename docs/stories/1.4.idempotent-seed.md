# Story 1.4 — Idempotens seed script

## Status
Done

## Story
Mint fejlesztő, szeretném a seed adatot betölteni duplikáció nélkül (FR1, FR2).

## Acceptance Criteria
1. Beolvassa a `seed-customers.json`-t és upsert-el `(name, telepules)` alapján.
2. Kétszer futtatva a sorszám 15 marad (idempotens).
3. Minden sorhoz feloldja a `lat/lon`-t; ismeretlennél `null` + log, nem áll le.
4. Robusztus main-guard (közvetlen indításkor fut).

## Tasks / Subtasks
- [x] `src/seed.ts` — JSON olvasás, geokódolás, `prisma.customer.upsert`
- [x] `pathToFileURL` main-guard
- [x] Integrációs teszt: kétszeri futtatás → count 15

## Dev Notes
Upsert kulcs: `name_telepules` (a `@@unique` compound). Ismeretlen koordinátánál
`console.warn` + folytatás (FR5). A `location.city` → `telepules`.

## Dev Agent Record
Implementálva; idempotencia integrációs teszttel igazolva.
