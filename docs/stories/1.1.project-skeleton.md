# Story 1.1 — Projekt-váz és eszközlánc

## Status
Done

## Story
Mint fejlesztő, szeretnék egy futtatható TS+Node projektet Postgresszel és
teszt-runnerrel, hogy legyen mire építeni a további story-kat.

## Acceptance Criteria
1. `package.json` scriptekkel (build, seed, start, test); lockfile commitolva.
2. `tsconfig.json` és Vitest konfiguráció.
3. `docker-compose.yml` Postgreshez; `.env.example` a `DATABASE_URL`-lel.
4. Prisma bekötve; `prisma migrate` üres `customers` táblát hoz létre.

## Tasks / Subtasks
- [x] `package.json` + scriptek + függőségek (express, prisma, vitest, tsx)
- [x] `tsconfig.json`, `vitest.config.ts`
- [x] `docker-compose.yml` (postgres:16), `.env.example`
- [x] Prisma init, `src/db.ts` kliens

## Dev Notes
Tech stack az architecture szerint: TS ^5, Node ≥20, Postgres 16, Prisma ^5,
Express ^4, Vitest ^1. Forrásfa: `src/`, `prisma/`, `test/`.

## Dev Agent Record
Implementálva az Epic 1 kód-commitban; migráció: `prisma/migrations/*_init`.
