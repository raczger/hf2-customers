# Story 2.3 — `GET /customers/by-distance`

## Status
Done

## Story
Mint fogyasztó, szeretném az ügyfeleket Budapesttől növekvő távolság szerint.

## Acceptance Criteria
1. Minden elem tartalmaz `distanceKm`-t (1 tizedesre kerekítve) (FR7).
2. Növekvő rendezés; budapestiek elöl (`0.0`) (FR8).
3. Ismeretlen koordinátájúak a végén `distanceKm: null`.
4. Holtverseny esetén `name` szerint.
5. Hibánál `500` JSON hibaválasz.

## Tasks / Subtasks
- [x] `src/geo/distance.ts` — `distanceKm` számítás + rendezés
- [x] `GET /customers/by-distance` a `src/server.ts`-ben
- [x] Unit teszt a rendezésre (null a végén, holtverseny `name`)

## Dev Notes
Budapest referencia a `reference.ts`-ből. Rendezés: null `distanceKm` a végére,
egyébként ASC; egyenlőségnél `name` szerint (FR8). Kerekítés 1 tizedesre.

## Dev Agent Record
Implementálva; rendezés unit-tesztelve, végpont integrációs teszttel.
